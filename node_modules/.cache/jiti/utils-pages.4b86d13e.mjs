"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.docsFolderExists = docsFolderExists;exports.landingPageExists = landingPageExists;var _nodeFs = await jitiImport("node:fs");
var _ufo = await jitiImport("ufo");

/**
 * Checks if the user has their own index.vue file in the pages folder.
 * When true, the layer should not define the landing collection and / route.
 */
function landingPageExists(rootDir) {
  const vueLandingPath = (0, _ufo.joinURL)(rootDir, 'app', 'pages', 'index.vue');
  return (0, _nodeFs.existsSync)(vueLandingPath);
}

/**
 * Checks if a docs folder exists in the content directory.
 * When true, docs should be prefixed with /docs and only include files from the docs folder.
 */
function docsFolderExists(rootDir, locale) {
  const docsPath = locale ?
  (0, _ufo.joinURL)(rootDir, 'content', locale, 'docs') :
  (0, _ufo.joinURL)(rootDir, 'content', 'docs');
  return (0, _nodeFs.existsSync)(docsPath);
} /* v9-08842f23af0dee69 */
